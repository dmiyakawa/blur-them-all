# Blur Them All - Apache Virtual Host Configuration Example
# Apache 2.4 用の設定例

# この設定ファイルは参考用です。実際の環境に合わせて調整してください。

<VirtualHost *:80>
    # サーバー名（実際のドメイン名に変更してください）
    ServerName blur-them-all.example.com
    ServerAlias www.blur-them-all.example.com

    # 管理者のメールアドレス
    ServerAdmin admin@example.com

    # ドキュメントルート（ビルドした dist/ ディレクトリのパス）
    DocumentRoot /var/www/blur-them-all/dist

    # ディレクトリ設定
    <Directory /var/www/blur-them-all/dist>
        # アクセス許可
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted

        # .htaccess ファイルの有効化
        AllowOverride All

        # デフォルトのインデックスファイル
        DirectoryIndex index.html
    </Directory>

    # ログファイル
    ErrorLog ${APACHE_LOG_DIR}/blur-them-all-error.log
    CustomLog ${APACHE_LOG_DIR}/blur-them-all-access.log combined

    # 必要なモジュールの有効化を確認してください:
    # - mod_rewrite (URL rewriting)
    # - mod_deflate (Gzip compression)
    # - mod_expires (Browser caching)
    # - mod_headers (Security headers)
    #
    # Ubuntu/Debian の場合:
    # sudo a2enmod rewrite deflate expires headers
    # sudo systemctl restart apache2
    #
    # CentOS/RHEL の場合:
    # これらのモジュールは通常デフォルトで有効になっています
</VirtualHost>

# HTTPS 設定例 (推奨)
# SSL証明書を取得してから有効にしてください
#
# <VirtualHost *:443>
#     ServerName blur-them-all.example.com
#     ServerAlias www.blur-them-all.example.com
#     ServerAdmin admin@example.com
#
#     DocumentRoot /var/www/blur-them-all/dist
#
#     <Directory /var/www/blur-them-all/dist>
#         Options -Indexes +FollowSymLinks
#         AllowOverride All
#         Require all granted
#     </Directory>
#
#     # SSL 設定
#     SSLEngine on
#     SSLCertificateFile /path/to/your/certificate.crt
#     SSLCertificateKeyFile /path/to/your/private.key
#     SSLCertificateChainFile /path/to/your/chain.crt
#
#     # SSL セキュリティ設定
#     SSLProtocol all -SSLv2 -SSLv3 -TLSv1 -TLSv1.1
#     SSLCipherSuite HIGH:!aNULL:!MD5
#     SSLHonorCipherOrder on
#
#     ErrorLog ${APACHE_LOG_DIR}/blur-them-all-ssl-error.log
#     CustomLog ${APACHE_LOG_DIR}/blur-them-all-ssl-access.log combined
# </VirtualHost>

# HTTP から HTTPS へのリダイレクト (HTTPS を有効にした場合)
#
# <VirtualHost *:80>
#     ServerName blur-them-all.example.com
#     ServerAlias www.blur-them-all.example.com
#
#     Redirect permanent / https://blur-them-all.example.com/
# </VirtualHost>
